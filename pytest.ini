[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (no I/O)
    integration: Integration tests (need running API)
    slow: Slow-running tests
